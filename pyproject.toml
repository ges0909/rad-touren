[project]
name = "rad-touren"
version = "0.0.0"
description = "Workspace root — not installable"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "lib",
    "mcp/*",
    "app/backend",
]
